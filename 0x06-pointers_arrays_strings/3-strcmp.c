#include <string.h>

/**
 * _strcmp - Compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 0 if 0s1 == @s2;
 *			x > 0 if @s1 > @s2
 *			x < 0 if @s1 < @s2
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
