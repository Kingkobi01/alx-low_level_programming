#include <stdio.h>

/**
 * main - Prints a line on the terminal using the printf() function.
 *
 * Return - 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
