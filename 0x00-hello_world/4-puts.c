#include <stdio.h>
#include <errno.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle\n");
	putchar(errno);
	return (0);
}
