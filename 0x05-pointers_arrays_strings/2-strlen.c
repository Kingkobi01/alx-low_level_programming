#include <stdio.h>
#include <string.h>

/**
 * _strlen - Returns the length of a string.
 *
 * @s: Pointer if char param
 *
 * Return: int, length of the string
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}

